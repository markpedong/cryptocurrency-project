import styled from "styled-components";
import "./font.scss";

export const HeaderTitleStyle = styled.p`
  margin-block-end: 0;
  color: var(--gray-9);
  font-size: 0.8rem;
  font-weight: 600;
`;

export const HeaderDescStyle = styled.p`
  color: blue;
  margin-block-end: 0;
  margin-inline-start: 0.5rem;
  font-size: 0.8rem;
  font-weight: 700;
`;

export const CryptoHeaderDetailsStyle = styled.p`
  color: var(--gray-9);
  font-size: 1rem;
  font-weight: 800;
  margin-block: 0;
  max-width: 100%;
`;

export const CryptoDescDetailsStyle = styled.p`
  color: var(--gray-9);
  font-size: 0.8rem;
  font-weight: 600;
  max-width: 100%;
`;
